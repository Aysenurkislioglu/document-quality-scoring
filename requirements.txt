# Document Quality Scoring — Python bağımlılıkları
#
# Bu dosya aşama aşama büyütülecektir: her yeni modül (glare, darkness, skew,
# occlusion, scoring) eklendiğinde gerekli paketler buraya eklenir.

# --- Blur modülü (tamamlandı) ---
opencv-python-headless>=4.9
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
scipy>=1.12
Pillow>=10.2

# --- Occlusion modülü (tamamlandı) ---
pytesseract>=0.3.10
# Not: sistemde tesseract-ocr + tesseract-ocr-tur paketlerinin kurulu olması gerekir
# (apt-get install tesseract-ocr tesseract-ocr-tur)

# --- Web arayüzü (Aşama 5 — basit skorlayıcı demo) ---
Flask>=3.0

# --- Occlusion Aşama 2: ML tabanlı, renkten bağımsız kapanma tespiti ---
scikit-learn>=1.4
joblib>=1.3
# Not: eğitilmiş model src/occlusion/models/occlusion_rf.joblib olarak repo'da
# tutuluyor; yeniden eğitmek için experiments/occlusion/train_occlusion_classifier.py
